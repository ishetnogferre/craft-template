<div class="grid__site">
  <div class="col-start-1 col-span-14 grid grid-cols-14 gap-4">
    {% for batch in module.children|batch(1) %}
      {% for block in batch %}
        <div class="col-span-full">
          {% include '_components/widgets/' ~ block.type.handle %}
        </div>
      {% endfor %}
    {% endfor %}
  </div>
</div>