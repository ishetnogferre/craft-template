<div class="grid__site">
  <div class="col-start-2 col-span-12 grid grid-cols-12 gap-x-4">
    {% for batch in children|batch(3) %}
      {% for block in batch %}
        <div class="overflow-hidden col-span-4">
        	{% block innerModule %}
        	{% endblock %}
        </div>
      {% endfor %}
    {% endfor %}
  </div>
</div>